"use client";
import React, { useEffect } from "react";

const HomePage = () => {
  // Chuyển hướng khi không có userId
  useEffect(() => {}, []);
  return <div>HomePage</div>;
};

export default HomePage;
